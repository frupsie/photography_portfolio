// ─── Social links ────────────────────────────────────────────────────────────
// Single source of truth — used by the Footer (every page) and the About
// page's "Follow on Instagram" link, so a handle only ever needs updating
// here. FILL THESE IN to switch a channel on; anything left blank renders
// nothing on either page, so it's safe to deploy as-is.
//   instagram: your handle WITHOUT the @  e.g. 'jaydenng.photo'
//   email:     the address to show        e.g. 'hello@example.com'
export const SOCIAL = {
  instagram: 'meelo_dinosaur',
  email: '',
};
